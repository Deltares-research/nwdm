contents:

etl: folder contains the pentaho scripts/workflow.
     this workflow is the original setup of the database. 

update_scripts: scripts that were used to updated the database

data: the data will be only locally. 
